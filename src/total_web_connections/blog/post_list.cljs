(ns total-web-connections.blog.post-list)

(def Posts
  {})

; (defn get-post-by-id [id]
;   (if id
;     ((keyword id) Posts)
;     false))
;
; (defn generate-all-posts []
;   (for [post Posts]
;     (Blog-card post)))
;
; (defn get-n-posts [n]
;   (let [post-sub (take n Posts)]
;     (for [post post-sub]
;       (Blog-card post))))
